import { View, SafeAreaView, Text } from 'react-native';
import styled from 'styled-components/native';

const Home = () => {
    return (
        <View>
            <SafeAreaView>
                <Text>Home</Text>
            </SafeAreaView>
        </View>
    
    );
};

export default Home;